/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            hostname: "n2bgxkwimh1mp7ae.public.blob.vercel-storage.com"
        }]
    }
};

export default nextConfig;
